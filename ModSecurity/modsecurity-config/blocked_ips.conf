# Blocked IP addresses - ModSecurity format
# Each IP should be in a SecRule format

SecRule REMOTE_ADDR "@streq 192.168.15.3" \
    "id:900002,phase:1,deny,status:403,msg:'Blocked IP: 192.168.15.3',severity:'CRITICAL'"