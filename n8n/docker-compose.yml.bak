version: '3.8'

services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    restart: always
    dns:
      - 129.168.15.3
    ports:
      - 5678:5678
    environment:
      #- N8N_PATH=/n8n/
        # - N8N_ENDPOINT_REST=n8n/rest
        #- N8N_ENDPOINT_WEBHOOK=n8n/webhook
      - WEBHOOK_URL=http://sbihi.soar.ma:5678/
        #- N8N_BASIC_AUTH_ACTIVE=true
          #- N8N_BASIC_AUTH_USER=admin
      - N8N_SECURE_COOKIE=false 
          #- N8N_BASIC_AUTH_PASSWORD=8<@704hrHI]V
      # Ajoute ici d'autres variables d'environnement si besoin
    volumes:
      - n8n_data:/home/node/.n8n
    networks:
      - single-node_default

volumes:
  n8n_data:

networks:
  single-node_default:
    external: true
